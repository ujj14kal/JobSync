# ─── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
pydantic==2.8.2
pydantic-settings==2.3.4

# ─── Database ─────────────────────────────────────────────────
supabase==2.5.3
asyncpg==0.29.0

# ─── AI / ML ──────────────────────────────────────────────────
groq==0.9.0
sentence-transformers==3.0.1
numpy==1.26.4
scikit-learn==1.5.1
joblib==1.4.2              # model serialisation
# torch — installed by Dockerfile via CPU-only wheel index (pytorch.org/whl/cpu)
#         Do NOT add torch here; pip would upgrade it from PyPI pulling in CUDA (+1.5 GB)
#         Local dev: pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
# transformers — pulled automatically as a sentence-transformers dependency
# xgboost — optional; model_trainer.py has graceful fallback to LogReg

# ─── Resume Parsing ───────────────────────────────────────────
pdfplumber==0.11.2
python-docx==1.1.2
PyMuPDF==1.24.7

# ─── Web Scraping ─────────────────────────────────────────────
playwright==1.45.0
playwright-stealth==1.0.6
beautifulsoup4==4.12.3
lxml==5.2.2
httpx==0.27.0
aiohttp==3.9.5

# ─── Firebase ─────────────────────────────────────────────────
firebase-admin==6.5.0

# ─── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
tenacity==8.4.1
structlog==24.4.0
cachetools==5.4.0

# ─── Payments & Claude ────────────────────────────────────────
anthropic==0.40.0          # Claude Sonnet — paid features only
razorpay==1.4.2            # Indian payment gateway (2% fee, no monthly cost)
