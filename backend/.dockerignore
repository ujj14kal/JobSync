data/
logs/
# models/ is intentionally NOT excluded — scorer.pt + tokenizer.json are baked
# into the image so Cloud Run needs zero GitHub API calls on cold start.
# Only exclude large legacy files if present.
models/*.log
notebooks/
scripts/
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
*.md
.git
.gitignore
.pytest_cache/
.coverage
*.egg-info/
.mypy_cache/
railway.toml
run_training_pipeline.py
